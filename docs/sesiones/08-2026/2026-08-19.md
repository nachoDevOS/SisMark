# Sesión de trabajo — 2026-08-19

> Registro de trabajo realizado por **nachoDev**. Rama: `master`.
>
> **Documento abierto: esto es la agenda del día, no el registro de lo hecho.**
> Cada trabajo arranca como plan y se completa a medida que se cierra. Lo que no
> se llegue a hacer se pasa a la sesión siguiente en vez de borrarse.
>
> Continúa el `2026-08-18`, que dejó la sincronización automática de los relojes
> terminada y probada pero **sin desplegar**. El trabajo del día es ponerla a
> correr en producción, que es donde se descubre lo que falta.
>
> Punto de partida: commit `15cba62` en `master`, sin subir. 578 pruebas en verde.

---

## Trabajo 1 — Subir y desplegar en Coolify

**Commit:** `(pendiente)`

### Qué hay que hacer

El commit `15cba62` está local. Coolify despliega desde el repositorio, así que
mientras no se suba, el servidor no tiene nada de esto.

```
git push
```

Y en Coolify, al crear o actualizar el recurso:

| Variable | Valor |
|---|---|
| `APP_KEY` | La propia del entorno (`php artisan key:generate --show`) |
| `APP_URL` | El dominio real, con `https` |
| `APP_TIMEZONE` | `America/La_Paz` |
| `APP_DEBUG` | `false` |
| `DB_HOST` · `DB_DATABASE` · `DB_USERNAME` · `DB_PASSWORD` | La base MySQL del recurso |
| `DEVICE_SERVICE_TOKEN` | El mismo del microservicio |
| `MAMORE_API_URL` · `MAMORE_API_KEY` | La API de datos personales |
| `SISMARK_API_KEY` | La que consume Mamoré |
| `SISMARK_SCHEDULER` | **No declararla.** El `Dockerfile` ya trae `true` |

Puerto a exponer: **8000**. Ruta de salud: **`/up`**.

### Verificación

En los logs del contenedor tienen que aparecer, en este orden:

```
[sismark] Microservicio de biométricos responde.
[sismark] Arrancando el planificador de tareas (sincronización automática de equipos)…
```

Y las tres migraciones nuevas corridas:

```
2026_08_17_155537_agregar_sincronizacion_automatica_a_equipos
2026_08_18_102437_agregar_dias_a_sincronizacion_de_equipos
2026_08_18_110132_agregar_ultimo_exito_a_sincronizacion_de_equipos
```

---

## Trabajo 2 — Confirmar que el servidor llega a los relojes

**Commit:** `(sin código, salvo que haya que cambiar el despliegue)`

### El problema

Es el riesgo que puede tirar abajo todo lo demás, y no se resuelve con código.

Los relojes están en direcciones privadas de la LAN del organismo —el que está en
uso hoy es `192.168.14.187`—. Si Coolify corre en un VPS de internet, **no hay ruta
posible hasta ahí**. La sincronización automática va a fallar en cada corrida
aunque el sistema esté impecable, y el síntoma va a ser un mensaje genérico de
«no se pudo conectar con el equipo» que se puede confundir con un problema del
reloj.

### Cómo se comprueba

Desde el contenedor ya desplegado:

```
docker exec -it sismark-app sh -c "nc -zv 192.168.14.187 4370"
```

### Si no conecta

El microservicio no puede vivir dentro del contenedor de la aplicación. Se separa:

| Pieza | Dónde va |
|---|---|
| Laravel + MySQL | Coolify |
| `device-service` | Una máquina **dentro de la red** que vea los relojes |

Y en el contenedor de Laravel:

```
SISMARK_DEVICE_SERVICE=false
DEVICE_SERVICE_URL=https://<host-del-microservicio-en-la-red>
DEVICE_SERVICE_TOKEN=<el mismo de los dos lados>
```

El microservicio **no se publica a internet**: solo tiene que ser alcanzable desde
el servidor de la aplicación, y el token es lo único que lo protege.

---

## Trabajo 3 — Cargar los horarios reales de cada reloj

**Commit:** `(sin código)`

### Qué hay que hacer

Hasta ahora la única configuración cargada fue de prueba (`10:47`, todos los días).
Con Recursos Humanos hay que definir, reloj por reloj:

- **Qué días.** Lo normal es lunes a viernes; los relojes de turnos rotativos o de
  lugares con guardia de fin de semana van con los siete.
- **Qué horas.** Conviene una por cada bloque de marcación ya cerrado, no una sola
  al final del día: si el reloj se cae a media tarde, lo de la mañana ya entró.
  Punto de partida razonable: `08:30`, `13:00`, `19:00`.

Se carga en **Biométricos → editar el equipo → «Sincronizar automáticamente en días
y horas fijas»**.

### Verificación

Por cada equipo configurado, al día siguiente la ficha tiene que mostrar las dos
fechas al día:

```
Última corrida automática   19/08/2026 19:00
Último dato traído          19/08/2026 19:00
```

Si la segunda queda atrás, el reloj responde pero no entrega — y ahí la ficha ya
lo dice sola, con el aviso «El equipo no responde desde entonces».

---

## Trabajo 4 — Rotar el log del planificador

**Commit:** `(pendiente)`

### Problema

`schedule:work` escribe unas dos líneas por minuto en
`storage/logs/scheduler.log`: alrededor de **400 KB por día**, sin techo.

El entrypoint lo trunca en cada arranque del contenedor, así que con los
redespliegues normales de Coolify no molesta. Pero un contenedor que quede meses
sin tocar acumula, y el volumen de `storage` no está para eso.

### Camino propuesto

Antes de escribir código, medir cuánto crece de verdad en el servidor. Si molesta,
la opción más barata es que la salida vaya al stdout del contenedor —que es lo que
Coolify ya muestra y Docker ya rota— en vez de a un archivo. Rompe la simetría con
el microservicio, que sí escribe a archivo, así que conviene decidir para los dos
juntos.

---

## Trabajo 5 — Deudas chicas anotadas en la revisión

**Commit:** `(pendiente)`

Salieron de la revisión completa del sistema del `2026-08-18`. Ninguna es urgente;
son de las que muerden cuando ya nadie se acuerda de por qué están.

| Deuda | Dónde | Qué pasa |
|---|---|---|
| Bloque `AWS_*` duplicado | `.env.example` líneas 80-84 y 108-114 | Gana el último y funciona, pero `AWS_USE_PATH_STYLE_ENDPOINT` aparece con dos valores distintos. Ya estaba anotado el `2026-08-18` para el `.env` real |
| El listado de horarios no rellena el buscador | `DiaTurnoController::index` lee `?buscar=`, el AJAX manda `?q=` | Entrar por `/horarios?q=mañana` filtra bien pero deja el campo vacío. Cosmético |
| `--equipo` corre sin `--forzar` | `SincronizarEquipos::equipos()` | Con `--equipo=N` se saltea `activo`, la automática y el horario, aunque la ayuda diga que eso lo hace `--forzar`. Además pisa `sync_ultimo_automatico`, o sea que una corrida manual mueve la ventana de la automática |

---

## Trabajo 6 — Avisar en el escritorio del reloj que no entrega

**Commit:** `(pendiente)`

### Idea

La ficha de cada equipo ya distingue «corrió» de «trajo datos», pero hay que
entrar equipo por equipo para verlo. El escritorio tiene la tarjeta «Equipos fuera
de línea», que se apoya en `en_linea` —lo que contestó el último «Probar
conexión»— y no en si la sincronización automática está trayendo algo.

Un reloj puede figurar en línea y llevar cuatro días sin entregar una marcación.
Eso es exactamente lo que la columna `sync_ultimo_exito` permite detectar y hoy no
se muestra en ningún lado más que en la ficha.

Queda como propuesta, para decidir con Recursos Humanos si sirve antes de
construirlo.

---

## Informe para presentación (copiar/pegar en Word)

**Informe de trabajo — 19/08/2026**
**Responsable:** nachoDev

**Actividad:** [completar al cierre del día]

**Problema detectado:**
[completar]

**Solución aplicada:**
[completar]

**Detalle de archivos modificados:**
1. [completar]

**Resultado:** [completar]

---

*Documento de sesión — nachoDev — Gobierno Autónomo Departamental del Beni.*
