# Sesión de trabajo — 2026-08-18

> Registro de trabajo realizado por **nachoDev**. Rama: `master`.
>
> Se cierra el circuito de licencias que quedó a medias el `2026-08-15`: aquel día
> las solicitudes ya entraban en «Pendiente», pero **no había forma de resolverlas
> desde la interfaz** y el funcionario no podía adjuntar su certificado.
>
> Ocho trabajos: (1) la API acepta el respaldo, (2) la ficha de la solicitud,
> (3) el botón de aprobar/rechazar con su trazabilidad, (4) el listado que muestra
> una licencia por pedido y no una por día, (5) lo que la API le tuvo que dar al
> perfil del funcionario, (6) de dónde salió cada licencia y qué se puede hacer con
> ella, (7) el arreglo de un día rechazado que quedaba bloqueado —sin perder el
> historial—, y (8) el aviso de pendientes más la limpieza de las tablas.
>
> Cierra con tres trabajos de otro módulo, los biométricos: (9) cada reloj se
> sincroniza solo a las horas que se le configuren desde su ficha, con el
> planificador corriendo dentro del contenedor; (10) esa configuración pasa a
> incluir los días de la semana; y (11) un equipo que estuvo caído recupera al
> volver todos los días que se perdió, más los seis tests viejos que estaban en
> rojo desde hacía semanas.
>
> Contraparte en Mamoré: ver la sesión `2026-08-18` de ese repositorio.

---

## Trabajo 1 — La API acepta el respaldo de la solicitud

**Commit:** `(pendiente)` — `feat(api): la solicitud de licencia puede traer su respaldo`

### Problema

Recursos Humanos podía adjuntar el certificado al cargar una licencia desde la
pantalla, pero el funcionario que la solicitaba desde Mamoré no. Quedó anotado
como pendiente en la sesión del `2026-08-15`.

Sin respaldo, quien tiene que aprobar mira una fila con un motivo escrito a mano
y nada más: no tiene con qué decidir.

### Archivos modificados

| Archivo | Cambio |
|---|---|
| `app/Http/Controllers/Api/SolicitudLicenciaController.php` | Acepta `respaldo`, lo sube con `RespaldoDocumento` y normaliza `tCompleto`. |
| `app/Http/Resources/LicenciaApiResource.php` | Expone `nota`, `revisadaEl` y `respaldoNombre`. |

### Archivos creados

| Archivo | Cambio |
|---|---|
| `tests/Feature/Api/SolicitudLicenciaApiTest.php` | 8 tests del endpoint de solicitudes, que no tenía ninguno. |

### Solución

Mismos límites que el alta de Recursos Humanos —imagen o PDF, hasta 5 MB— y
**opcional**, por el mismo motivo que allá: el certificado se presenta al volver,
así que exigirlo dejaría sin pedir licencia a quien todavía no lo tiene.

El archivo se sube **después** de resolver los turnos, no antes. Si se subiera
primero, cada solicitud que no llega a anotar nada —el caso más común, «no tenés
turnos asignados en esas fechas»— dejaría un archivo huérfano en el bucket.

**`tCompleto` hay que normalizarlo.** Con respaldo la petición viaja como
`multipart/form-data`, y ahí no hay booleanos: llega el texto `"0"`. La regla
`required_if:tCompleto,false` compara contra un booleano real, así que **no
dispara**, y una licencia parcial habría pasado sin horas de entrada ni de salida.
Un `$request->merge(['tCompleto' => $request->boolean('tCompleto')])` antes de
validar lo resuelve para los dos formatos.

**Antes:**
```php
$datos = $request->validate([
    'tCompleto' => ['required', 'boolean'],
    'lEntra' => ['nullable', 'required_if:tCompleto,false', 'date_format:H:i'],
    // sin 'respaldo'
]);
```

**Después:**
```php
// En multipart no hay booleanos: sin esto, `required_if` no dispara.
$request->merge(['tCompleto' => $request->boolean('tCompleto')]);

$datos = $request->validate([
    'tCompleto' => ['required', 'boolean'],
    'lEntra' => ['nullable', 'required_if:tCompleto,false', 'date_format:H:i'],
    'respaldo' => ['nullable', 'file', 'mimes:jpg,jpeg,png,pdf', 'max:5120'],
]);
```

La API devuelve además `nota` (por qué se resolvió así), `revisadaEl` y
`respaldoNombre`. El archivo **no** se sirve por esta API: al funcionario le
alcanza con confirmar que su certificado llegó, y el documento se mira acá.

### Verificación

```
php artisan test tests/Feature/Api/SolicitudLicenciaApiTest.php
→ 8 tests, 8 pasan
```

Cubren: que la solicitud quede «Pendiente» y sin `registerUser_id`, que una
pendiente no justifique la ausencia, que el respaldo llegue al bucket, que sea
opcional, que se rechace un `.xlsx` y un PDF de 6 MB, que la licencia parcial en
multipart siga exigiendo las horas, y que sin la clave compartida no se registre
nada.

---

## Trabajo 2 — Ficha de la solicitud

**Commit:** `(pendiente)` — `feat(licencias): ficha de la solicitud, previa a resolverla`

### Problema

Las licencias no tenían ficha: se veían como una fila del listado y nada más. Para
aprobar hace falta mirar antes qué se está aprobando —qué días abarca, con qué
motivo y con qué respaldo—, y eso no cabe en una fila.

### Archivos creados

| Archivo | Cambio |
|---|---|
| `resources/views/licencias/show.blade.php` | La ficha: solicitud, días alcanzados y panel de resolución. |

### Archivos modificados

| Archivo | Cambio |
|---|---|
| `app/Http/Controllers/LicenciaController.php` | `show()`; `list()` e `index()` aceptan el filtro por estado. |
| `app/Models/Licencia.php` | Constantes de estado, scopes `deLaSolicitud()` y `pendientes()`, relación `revisor()`. |
| `app/Policies/LicenciaPolicy.php` | `view()`. |
| `app/Policies/RolePolicy.php` | `View:Licencia` y la habilidad nueva `Approve`. |
| `routes/web.php` | `show` en el resource. |
| `resources/views/licencias/list.blade.php` | Columna **Estado** y botón **Ver**. |
| `resources/views/licencias/index.blade.php` | Filtro por estado. |

### Solución

**La ficha muestra la solicitud entera, no la fila.** `RegistroLicencia` expande el
rango a una fila por día y turno: una licencia de cinco días son cinco filas. Si la
ficha mostrara una sola, Recursos Humanos tendría que abrir cinco pantallas para
resolver un pedido. Se entra por cualquier fila y se ve la tanda completa.

**Las filas hermanas se agrupaban por `ci` + `fechaPedido`.** `fechaPedido` la
escribe el servicio una sola vez para todo el alta, así que parecía identificar la
tanda sin columna nueva.

> **Esto se reemplazó en el Trabajo 4.** Medido contra la tabla real, la clave
> juntaba cosas que no van juntas: un grupo llegaba a 10.308 filas separadas por 20
> años. Hoy la clave es la columna `solicitud`.

La ficha rotula de dónde vino el pedido. Al principio se deducía de
`registerUser_id` nulo —la API anota con `Licencia::insert()`, que no dispara
eventos de modelo, así que el trait `RegistersUserEvents` no escribe esa columna—;
**en el Trabajo 6 pasó a ser un dato explícito**, la columna `origen`.

El listado ganó la columna **Estado** y un **filtro por estado**, que es cómo se
encuentran las solicitudes pendientes sin barrer un millón de licencias históricas
ya aprobadas. El filtro se valida contra la lista conocida: un estado inventado en
la URL devuelve el listado completo y no una tabla vacía sin explicación.

### Verificación

```
php artisan test --filter="Licencia|Solicitud|Procesador|Permisos"
→ 140 tests, 140 pasan
pint → passed
```

---

## Trabajo 3 — Aprobar y rechazar, con quién y cuándo

**Commit:** `(pendiente)` — `feat(licencias): aprobar o rechazar la solicitud del funcionario`

### Problema

Lo que faltaba para cerrar el circuito. Hasta hoy las solicitudes entraban en
«Pendiente» y **se quedaban ahí para siempre**: no había pantalla, ni acción, ni
siquiera se veía el estado en el listado. Y como `ProcesadorAsistencia` solo cuenta
las aprobadas, cada solicitud hecha desde Mamoré terminaba imputada como falta
mientras el funcionario la creía en trámite.

### Archivos creados

| Archivo | Cambio |
|---|---|
| `app/Http/Requests/RevisarLicenciaRequest.php` | Reglas y autorización de la resolución. |
| `database/migrations/2026_08_17_101500_agregar_revision_a_licencias.php` | `revisadoPor_id` y `revisadoEn`. |

### Archivos modificados

| Archivo | Cambio |
|---|---|
| `app/Http/Controllers/LicenciaController.php` | `aprobar()`, `rechazar()` y el `resolver()` que comparten. |
| `app/Policies/LicenciaPolicy.php` | `approve()`. |
| `routes/web.php` | `PATCH licencias/{licencia}/aprobar` y `/rechazar`. |
| `tests/Feature/LicenciaControllerTest.php` | 8 tests nuevos del circuito de resolución. |

### Solución

Dos botones en la ficha, sobre los días que siguen «Pendiente». **Aprobar y
rechazar son rutas distintas**, no un campo del formulario: así un envío
manipulado no puede convertir un rechazo en una aprobación.

**El motivo del rechazo es obligatorio.** Es lo que el funcionario lee en su
perfil; sin él se entera de que le negaron el permiso pero no de qué le faltó.
Va en `observacion`, una columna que ya existía en la tabla y que **ninguna
pantalla escribía**, así que no hizo falta agregarla.

**Se agregó `revisadoPor_id` y `revisadoEn`.** Una licencia aprobada justifica una
ausencia, y sin autor esa decisión no se le puede atribuir a nadie.
`registerUser_id` no servía: es quién dio el alta, y en lo que llega de Mamoré
viene nulo justamente porque el funcionario no tiene usuario acá.

**La resolución solo toca lo que sigue «Pendiente».** Si otro usuario resolvió la
misma solicitud mientras la pantalla estaba abierta, su decisión no se pisa: el
`update` filtra por estado y, si no afectó ninguna fila, se avisa.

```php
$afectados = Licencia::query()
    ->deLaSolicitud($licencia)
    ->pendientes()               // ← lo ya resuelto no se toca
    ->update([
        'estado' => $estado,
        'observacion' => $request->validated('observacion'),
        'revisadoPor_id' => $request->user()?->id,
        'revisadoEn' => now(),
    ]);
```

### El error que encontraron las pruebas

La condición «solo se resuelve lo Pendiente» estaba escrita **dentro de la
policy**, que es donde parecía corresponder. Dos tests la desmintieron: aprobar
una licencia ya aprobada devolvía 302 en vez de 403, y la ficha de una resuelta
seguía ofreciendo los botones.

La causa es el atajo de `AppServiceProvider`:

```php
Gate::before(fn ($user): ?bool => $user->hasRole('super_admin') ? true : null);
```

Devuelve `true` **sin llegar a ejecutar la policy**. Cualquier regla de negocio
escrita ahí se saltea justamente para super_admin, que es quien más usa esta
pantalla.

La condición se movió a `RevisarLicenciaRequest::authorize()`, que corre siempre,
y la vista la repite en un `@if` aparte del `@can`. La policy quedó con lo único
que le corresponde: el permiso.

**Es un patrón a tener en cuenta en todo el sistema** — ninguna policy de este
proyecto puede llevar reglas de estado mientras exista ese `Gate::before`.

### Verificación

```
php artisan test --filter="Licencia|Solicitud|Procesador|Permisos"
→ 140 tests, 140 pasan
pint → passed
```

Los 8 tests nuevos cubren: que la ficha muestre los tres días de la tanda y no
solo el abierto, que aprobar resuelva los tres de una, que **no alcance a otra
solicitud del mismo funcionario** pedida en otro momento, que rechazar exija el
motivo y lo guarde, que lo ya resuelto devuelva 403, que la ficha de una aprobada
no ofrezca resolverla, el filtro por estado del listado, y que sin
`Approve:Licencia` se pueda mirar pero no decidir.

Migración y permisos aplicados sobre la base de desarrollo:

```
php artisan migrate                              → DONE
php artisan db:seed --class=RolesAndPermissionsSeeder

Permisos Licencia: Approve, Create, Delete, View, ViewAny
Columnas nuevas:   revisadoPor_id, revisadoEn
```

### Nota sobre el suite completo

Al correr `php artisan test` entero fallan **7 tests** de `DashboardControllerTest`,
`DiaExcepcionalControllerTest` y `DiaTurnoControllerTest`. **Son anteriores a este
trabajo** y no tienen relación con licencias: esos listados se cargan por AJAX
(`div-results`) y los tests asertan el dato sobre el «shell» de la pantalla, que
solo dice «Cargando…». Ninguno de esos archivos figura en `git status`. Es la misma
familia de fallos anotada el `2026-08-15`, que entonces eran 6.

### Decisiones y pendientes

- **No se revierte una resolución desde la ficha.** Una licencia aprobada descuenta
  la ausencia, y darla vuelta después cambiaría hacia atrás reportes que Recursos
  Humanos ya firmó. Si hubo un error, la baja lógica es el camino.
- **El respaldo no se borra al eliminar.** Sigue el criterio que ya existía: la
  eliminación es lógica y las filas hermanas comparten el archivo.
- **Sigue abierto** si el funcionario puede volver a pedir tras un rechazo: el
  índice único `(ci, fecha, turno_id)` lo bloquea. Hoy tiene que pedirlo a Recursos
  Humanos.
- **Sin aviso al funcionario.** Se entera del resultado entrando a su perfil; no
  hay notificación.

---

## Trabajo 4 — El listado muestra una licencia por pedido, no una por día

**Commit:** `(pendiente)` — `feat(licencias): el listado agrupa los días de un mismo pedido`

### Problema

Pedir del **14 al 15 de agosto** deja **dos filas** en `licencias` —el alta expande
el rango a una fila por día y turno—, y el listado las mostraba como dos licencias
distintas. Daba a entender que hay que aprobarlas por separado.

Aprobar ya era un solo click desde el Trabajo 2 (la ficha agrupaba y el botón
resolvía los N días de una), pero la pantalla anterior contradecía eso.

### Archivos creados

| Archivo | Cambio |
|---|---|
| `database/migrations/2026_08_17_143000_agregar_solicitud_a_licencias.php` | `solicitud`, relleno del histórico y dos índices. |

### Archivos modificados

| Archivo | Cambio |
|---|---|
| `app/Services/RegistroLicencia.php` | Un ULID por alta **y por funcionario**. |
| `app/Console/Commands/MigrarLicenciasSia.php` | Reconstruye el pedido del SIA por `ci + fechaPedido + motivo`. |
| `app/Models/Licencia.php` | `scopeDeLaSolicitud()` por columna, `scopeIniciosDeSolicitud()`, `paginarPorSolicitud()`, `resumenDe()`. |
| `app/Http/Controllers/LicenciaController.php` | El listado consulta inicios; `destroy()` da de baja la solicitud entera. |
| `resources/views/licencias/list.blade.php` | Columnas **Periodo** y **Días**; una fila por pedido. |
| `database/factories/LicenciaFactory.php` | `solicitud` por defecto. |
| `app/Http/Controllers/PersonaController.php` | La solapa de licencias de la ficha también agrupa. |
| `resources/views/funcionarios/licencias-list.blade.php` | Periodo, días, estado y aviso de baja correcto. |

### Solución

Una columna `solicitud` (ULID) que `RegistroLicencia` escribe una vez por alta:

```
id     ci        fecha       turno_id  estado     solicitud
1042   7633685   2026-08-14  407       Pendiente  01K2X8M4QJ7VNPB3…
1043   7633685   2026-08-15  408       Pendiente  01K2X8M4QJ7VNPB3…   ← la misma
```

La base sigue con una fila por día, y no se discute: el índice único
`(ci, fecha, turno_id)` es lo que impide licenciar dos veces el mismo turno,
`ProcesadorAsistencia` cruza día contra turno, y una licencia parcial lleva horas
propias por turno. Lo que cambia es la pantalla.

**Un ULID por alta y por funcionario, no por alta.** Un feriado alcanza a más de
400 personas; con un solo identificador el listado mostraría una fila para 400
legajos y la ficha —que muestra a una persona— no sabría a cuál.

### Por qué no alcanzaba `ci` + `fechaPedido`

Fue el primer intento, porque `RegistroLicencia` escribe `fechaPedido` una vez por
alta. Medido sobre las 1.110.345 filas reales:

```
Grupos (ci + fechaPedido):                 213.956
Promedio de filas por grupo:                   5,2
Máximo:                                     10.308   ←
Grupos con más de 60 filas:                  1.854
Filas con fechaPedido a las 00:00:00:      688.216   (62%)
Mayor separación de fechas en un grupo:  7.213 días  (~20 años)
```

El SIA guardó el 62% sin hora, así que todo lo que una persona pidió cualquier día
de 2021 caía en el mismo grupo. Era además un error latente introducido en el
Trabajo 2: abrir la ficha de una licencia histórica habría traído 10.308 filas.

### El problema de rendimiento, y por qué hay dos columnas y no una

Con `solicitud` ya escrita, el listado se armó con `GROUP BY solicitud`. Medido
contra la tabla real:

```
Listado viejo (sin agrupar)                    1,44 s   ← línea base
GROUP BY COALESCE(solicitud, id)              14,61 s
GROUP BY solicitud (con índice)                3,27 s
  └─ EXPLAIN: Using temporary; Using filesort · 1.098.094 filas
```

No es cuestión de índice: para mostrar las 10 solicitudes más recientes hay que
agregar el millón de grupos y ordenarlos, y el `COUNT` del paginador repite el
mismo trabajo. Un listado 10× más lento no es una opción.

El listado quedó resolviéndolo **en dos pasos**, sin agrupar la tabla entera:

1. Qué solicitudes cruzan el filtro, con su fecha más reciente —`GROUP BY
   solicitud ORDER BY MAX(fecha) DESC`—, paginado. Eso es lo que se cuenta: una
   licencia de cinco días es **una**.
2. De esas diez, la fila que abre cada una, con un `whereIn` delante.

Está en `Licencia::paginarPorSolicitud()`, y lo usan el listado general y la solapa
de la ficha del funcionario.

### El camino que se probó y se descartó

Se llegó ahí después de dos intentos que no sirvieron, los dos medidos contra la
tabla real:

| Intento | Sin filtro | Buscando |
|---|---|---|
| `GROUP BY COALESCE(solicitud, id)` | 14,6 s | — |
| `GROUP BY solicitud` con índice | 3,3 s | — |
| Una columna `esInicio` que marcara la fila | 0,4 s | 1,5 – 5,2 s |
| Subconsulta suelta `WHERE id = (SELECT … LIMIT 1)` | 0,0 s | **ABORTA (>45 s)** |
| **Dos pasos (el que quedó)** | **4,1 s** | **0 – 3,6 s** |

La columna `esInicio` llegó a implementarse: es la más rápida, pero es un dato
denormalizado que dos escritores tienen que mantener sincronizado y que, si queda
mal, hace desaparecer una solicitud del listado o la duplica. Se sacó a pedido, y
la subconsulta suelta —que parecía el reemplazo— se cae con el buscador: al filtrar
por algo poco frecuente no encuentra diez coincidencias temprano y termina
evaluándose en el millón de filas.

Los dos pasos no tienen ese problema porque el `GROUP BY` corre solo sobre lo ya
filtrado, y la subconsulta va siempre acotada a las diez solicitudes de la página.
El costo se concentra en la página sin filtrar, que es la página 1 de 26.000 y
nadie recorre.

**El índice `(solicitud, fecha)` no es opcional.** En una reversión se perdió y la
consulta pasó de 0,0 s a **21 s**: sin él, la subconsulta cae en el índice de
`fecha` y para cada fila candidata recorre las fechas buscando la solicitud.

### El histórico también se agrupa

Al principio cada fila del SIA quedaba como su propia licencia. Usándolo apareció el
problema: un memorándum de rotación salía **487 veces seguidas** en el listado, una
por día.

El SIA no guarda «el pedido», pero sí de qué alta salió cada fila —mismo carnet,
mismo momento del pedido y mismo motivo—, y eso alcanza. Medido sobre las
1.110.346 filas: **256.422 solicitudes, 4,3 filas de promedio**. Los grupos grandes
son licencias que de verdad lo son:

```
CUARENTENA TOTAL, abril a junio 2020   →  178.278 filas en 5.245 solicitudes
                                          (una por funcionario)
Memorándum retroactivo de 20 años      →   10.308 filas, sin huecos > 3 días
MEMO ROTACION A SEDEGES RT0353         →      487 filas en 2 solicitudes
                                          (una por año, distinto fechaPedido)
```

Agrupar solo por `ci + fechaPedido`, **sin el motivo**, sí los mezclaba.

El identificador se deriva de la clave con `sha2` en vez de sortearse: es estable
entre corridas, así que reejecutar `sia:migrar-licencias` no mueve los
agrupamientos ni necesita un mapa de 256.422 entradas en memoria.

### La baja pasa a ser de la solicitud

`destroy()` da de baja los días del pedido, no la fila: borrar un día suelto de un
pedido de cinco dejaría una licencia con un agujero en el medio que nadie decidió.
En lo histórico, donde la fila **es** la licencia, se elimina una sola. Se recorre
fila por fila para que el trait de auditoría escriba quién y por qué en cada una.

### El aviso de baja que mentía

Al cambiar `destroy()` quedó un defecto en la solapa **Licencias** de la ficha del
funcionario, que no se había tocado: seguía listando un día por fila y su botón de
baja decía *«Se elimina la licencia del 14/08/2026»* cuando en realidad se llevaba
los cinco días del pedido. Un diálogo de confirmación que miente sobre una acción
destructiva es peor que no tenerlo.

Esa pantalla pasó a agrupar igual que el listado general, con periodo, días,
estado y el aviso diciendo cuántos días se van.

### Verificación

```
php artisan test --filter="PersonaController|Licencia|Solicitud"
→ 151 tests, 151 pasan
php artisan test  → 479/486 (los 7 de siempre, ver la nota del Trabajo 3)
pint → passed
```

Seis tests nuevos: que un alta de tres días salga como una fila con su periodo,
que un alta para dos funcionarios dé dos solicitudes, que lo histórico siga suelto,
que la baja alcance a la solicitud entera y que una histórica no arrastre a las
demás, y que la paginación cuente solicitudes (12 pedidos de 2 días = 2 páginas, no
3).

### Nota sobre el despliegue

La migración **rellena más de un millón de filas** y crea tres índices: tardó 31 s
en desarrollo. El relleno va por bloques de 50.000 para no dejar la tabla tomada
durante todo el despliegue.

---

## Trabajo 5 — Lo que la API le da al perfil del funcionario

**Commit:** `(pendiente)` — `feat(api): licencias agrupadas, respaldo y baja de la solicitud del funcionario`

### Problema

Los Trabajos 2 a 4 arreglaron las pantallas de Recursos Humanos, pero el perfil
del funcionario en Mamoré consume esta API y seguía viendo lo de antes:

- Un pedido del 14 al 15 de agosto le llegaba como **dos licencias**, el mismo
  problema que se acababa de corregir acá.
- Del respaldo solo recibía el **nombre**: el archivo vive en nuestro bucket y es
  privado, así que no había cómo abrirlo desde allá.
- Y no había forma de que **diera de baja** un pedido hecho por error; tenía que
  pedírselo a Recursos Humanos.

### Archivos modificados

| Archivo | Cambio |
|---|---|
| `app/Http/Controllers/Api/AsistenciaFuncionarioController.php` | `licencias()` agrupa por solicitud y ordena al revés; `respaldo()` nuevo. |
| `app/Http/Controllers/Api/SolicitudLicenciaController.php` | `destroy()`: baja lógica de la solicitud propia. |
| `app/Http/Resources/LicenciaApiResource.php` | `id`, `hasta`, `dias`, `unSoloDia`, `estadoMixto`. |
| `routes/api.php` | Las dos rutas nuevas. |
| `tests/Feature/Api/*` | 15 tests nuevos entre los dos archivos. |

### Una licencia por pedido, y la más reciente primero

`licencias()` devuelve la fila que abre cada solicitud —`iniciosDeSolicitud()`— y
el recurso suma `hasta`, `dias` y `unSoloDia`.

**El rango elige qué pedidos entran, no los recorta.** Si un permiso empezó el 27
de julio y sigue el 3 de agosto, quien consulta agosto lo ve entero: por eso las
solicitudes se buscan por sus días dentro del rango y después se traen completas.

El orden pasó a `orderByDesc('fecha')`, el mismo del listado de Recursos Humanos:
lo último que el funcionario pidió es lo que viene a mirar, y en un rango largo
quedaba al fondo de la tabla.

### Dos endpoints donde el identificador deja de ser la cédula

`GET  funcionarios/{ci}/licencias/{licencia}/respaldo`
`DELETE funcionarios/{ci}/licencias/{licencia}`

Son los **únicos puntos de esta API donde lo que identifica el registro es el id
de una fila** y no la cédula. Y el id es un número corrido.

Sin comprobar nada, quien tenga la clave compartida pediría
`…/licencias/1/respaldo` e iría subiendo el número para bajarse los certificados
médicos de todo el personal —o para borrarles las licencias—. Por eso los dos
exigen que la fila sea de esa cédula, y responden **404 y no 403**: el mensaje no
puede confirmar que existe una licencia ajena con ese número.

La baja pide además que siga **«Pendiente»** (`422` si no): una licencia aprobada
justifica una ausencia y ya entró en reportes que Recursos Humanos firmó.

### La baja es lógica, y deja constancia

Se da de baja el pedido entero, fila por fila, con `deleted_at` y un motivo:

```
con motivo:  «Cancelada por el funcionario desde Mamoré: Me equivoqué de fecha.»
sin motivo:  «Cancelada por el funcionario desde Mamoré.»
```

`deleteUser_id` queda nulo —el funcionario no tiene usuario acá, igual que en el
alta—, así que el origen va en el texto y no se pierde.

**El motivo se guarda con `save()` antes del `delete()`.** `runSoftDelete()`
escribe solo `deleted_at` y `updated_at`, así que un atributo suelto no habría
llegado a la base y la columna habría quedado vacía sin dar error. Es lo mismo que
hace el trait de auditoría cuando sí hay un usuario en sesión.

### Verificación

En vivo, con Mamoré del otro lado:

```
GET    …/licencias?desde=2026-08-20&hasta=2026-08-31
  2026-08-20 → 2026-08-21 | dias=2 | unSoloDia=false | Pendiente | cert.pdf
  filas devueltas: 1                    (2 filas en la base)

GET    …/licencias/{id}/respaldo   (cédula correcta)  → 200 {"url":"https://…?X-Amz-…"}
GET    …/licencias/{id}/respaldo   (otra cédula)      → 404 «La licencia no existe.»

DELETE …/licencias/{id}            (otra cédula)      → 404
DELETE …/licencias/{id}            (con motivo)       → 200 «Tu solicitud se dio de baja (2 días).»
GET    …/licencias?desde=…                            → 0 visibles

En la base, las filas siguen ahí:
  id=1110378  20/08/2026  deleted_at=2026-08-17 09:02:46
      Cancelada por el funcionario desde Mamoré: Me equivoque de fecha, era la semana que viene.
```

```
php artisan test tests/Feature/Api/  → 44 tests, 44 pasan
php artisan test                     → 508/514 (los 6 de siempre)
pint → passed
```

---

## Trabajo 6 — De dónde salió cada licencia, y qué se puede hacer con ella

**Commit:** `(pendiente)` — `feat(licencias): columna origen y reglas de baja`

### Problema

Tres cosas que se veían mal en el listado:

1. **No se sabía de dónde venía cada licencia.** Se deducía de `registerUser_id`
   nulo, que es frágil: esa columna dice *quién* dio el alta, no *por dónde
   entró*, y queda nula por más de un motivo.
2. **Una rechazada se podía eliminar**, borrando la constancia de que se pidió y
   se negó —con su motivo, que es lo que el funcionario lee en su perfil—.
3. **Una pedida desde Mamoré se podía eliminar** en vez de resolverse.

### Archivos modificados

| Archivo | Cambio |
|---|---|
| `database/migrations/…_agregar_solicitud_a_licencias.php` | Columna `origen`, con el histórico marcado como `sia`. |
| `app/Models/Licencia.php` | Constantes de origen y `motivoParaNoEliminar()`. |
| `app/Services/RegistroLicencia.php` | `anotar()` acepta `origen`, con `propio` por defecto. |
| `app/Http/Controllers/Api/SolicitudLicenciaController.php` | Marca `mamore`. |
| `app/Console/Commands/MigrarLicenciasSia.php` | Marca `sia`. |
| `app/Http/Controllers/LicenciaController.php` | `destroy()` usa el criterio del modelo. |
| `resources/views/licencias/{list,show}.blade.php` | Columna **Origen**; el botón sigue el criterio. |
| `resources/views/funcionarios/licencias-list.blade.php` | Lo mismo en la ficha. |

### Solución

Tres orígenes, no dos: el histórico del SIA es un tercero y marcarlo «Propio»
sería mentira.

| Valor | De dónde |
|---|---|
| `propio` | Lo carga Recursos Humanos en este sistema |
| `mamore` | Lo pide el funcionario desde su perfil |
| `sia` | Vino de `sia:migrar-licencias` |

Las dos reglas de baja viven juntas en `Licencia::motivoParaNoEliminar()`, así el
botón y el servidor no pueden discrepar:

```
origen = mamore  → «La pidió el funcionario desde Mamoré: se aprueba o se
                    rechaza, no se elimina.»
estado = Rechazado → «Es la constancia de que se pidió y se negó.»
```

**No va en la policy**, por lo mismo que la aprobación: el `Gate::before` de
`AppServiceProvider` le concede todo al rol super_admin sin llegar a ejecutarla.

### Consecuencia a tener presente

Una licencia de Mamoré **nunca** se elimina desde este sistema, ni siquiera
aprobada por error. Quien puede darla de baja es el propio funcionario desde su
perfil, y solo mientras siga «Pendiente». Deshacer una aprobación equivocada no
tiene camino hoy.

### Además: al rechazar se vuelve a la ficha del funcionario

El pedido quedó cerrado y en su ficha no queda nada por hacer, mientras que en la
del funcionario se ve el resto de sus licencias. Elige la ficha local si la
persona está en `personas` y la ficha por cédula si no —el padrón lo manda Mamoré
y no todos tienen registro local—. Al aprobar se queda donde está, para poder
comprobar cómo quedaron los días.

### Verificación

```
php artisan test  → 527/533 (los 6 de siempre)
pint → passed
```

---

## Trabajo 7 — Un día rechazado se puede volver a pedir, sin perder el historial

**Commit:** `(pendiente)` — `fix(licencias): un día rechazado no queda bloqueado, y el pedido anterior se conserva`

### Problema

Apareció usándolo. El funcionario pidió del 10 al 12 de agosto, se lo rechazaron,
corrigió y volvió a pedir los mismos días:

```
«No se anotó ninguna solicitud: 0 licencia(s) anotada(s), 3 ya existían.»
```

Las tres «que ya existían» estaban **rechazadas**. El índice único
`(ci, fecha, turno_id)` las tenía reservadas y `RegistroLicencia::anotar()` las
contaba como ocupadas, así que **esos días quedaban bloqueados para siempre**: ni
el funcionario ni Recursos Humanos podían volver a licenciarlos.

Era el pendiente anotado desde el `2026-08-15`.

### El primer arreglo estuvo mal

Se resolvió **reusando** la fila rechazada: se reescribía con el pedido nuevo y
volvía a «Pendiente». Andaba, pero borraba el historial —qué se había pedido,
quién lo resolvió y con qué motivo—, que es justamente lo que el funcionario lee
en su perfil. Se detectó probándolo: un pedido rechazado del 10 al 14 desapareció
al volver a pedir esos días.

Este trabajo documenta la versión definitiva: **nada se reescribe**.

### Archivos modificados

| Archivo | Cambio |
|---|---|
| `database/migrations/…_agregar_solicitud_a_licencias.php` | La clave única pasa a `(ci, fecha, turno_id, solicitud)`. |
| `app/Services/RegistroLicencia.php` | `ocupados()` reemplaza a `existentes()`; se quitó la rama de restauración. |
| `app/Console/Commands/MigrarLicenciasSia.php` | La clave del upsert incluye `solicitud`. |

### Solución

La clave natural pasa a incluir el pedido:

```
antes    unique (ci, fecha, turno_id)
ahora    unique (ci, fecha, turno_id, solicitud)
```

Así **dos pedidos distintos del mismo día conviven**, y cada uno conserva su
desenlace. Un pedido nuevo es siempre una fila nueva.

Qué bloquea el día y qué no:

| Fila anterior | ¿Ocupa el día? | Qué pasa al volver a pedir |
|---|---|---|
| Aprobada | sí | «ya existía» |
| Pendiente | sí | «ya existía» |
| **Rechazada** | no | fila nueva; **la vieja queda** |
| **Dada de baja** | no | fila nueva; **la vieja queda** |

`ocupados()` consulta solo lo vigente y sin rechazar. Que no haya dos licencias
**vigentes** para el mismo turno lo cuida esa comprobación, no ya el índice.

La copia del SIA sigue siendo idempotente aunque la clave haya cambiado: su
`solicitud` se **deriva** de la fila (`sha2` de `ci|fechaPedido|motivo`) en vez de
sortearse, así que reejecutarla llega siempre al mismo valor.

### El detalle que lo hacía fallar

`existentes()` traía solo `['id', 'ci', 'fecha', 'turno_id', 'deleted_at']`. Sin
`estado` en el `select`, la comprobación leía `null` y ninguna fila se reconocía
como rechazada.

### Verificación

En vivo, controlado:

```
1) Pido el 07/09            → id=1110381  PEDIDO A  Pendiente
2) Recursos Humanos rechaza → id=1110381  PEDIDO A  Rechazado · «Falta certificado.»
3) Vuelvo a pedir el 07/09  → id=1110382  PEDIDO B  Pendiente

Quedan las dos filas:
  id=1110381 | PEDIDO A | Rechazado | nota=Falta certificado.
  id=1110382 | PEDIDO B | Pendiente | nota=-
```

```
php artisan test  → 528/534 (los 6 de siempre)
pint → passed
```

Pruebas nuevas: que una rechazada se pueda volver a pedir **conservando la
anterior**, que una dada de baja tampoco se pise, y que una aprobada y una
pendiente sigan bloqueando.

### Una trampa de las pruebas, no del sistema

Las primeras versiones de estos tests fallaban por el fixture:
`Licencia::factory()` guarda `fecha` con hora (`2026-08-03 00:00:00`) mientras que
el servicio la guarda como fecha (`2026-08-03`). En MySQL la columna es `DATE` y
las iguala; en SQLite —la base de las pruebas— son cadenas distintas, así que la
consulta de ocupados no encontraba la fila. Los tests pasaron a crear las
licencias **por la API**, que es el camino real.

---

## Trabajo 8 — Aviso de solicitudes por resolver, y limpieza de las tablas

**Commit:** `(pendiente)` — `feat(licencias): aviso de pendientes en la barra y ficha para el perfil`

### Problema

Cuatro cosas de uso diario:

1. **Nada avisaba que había solicitudes esperando.** Había que entrar al listado y
   filtrar para enterarse.
2. En la ficha del funcionario, la columna **Turno** engañaba: un pedido de cinco
   días puede abarcar turnos distintos y se mostraba solo el del primero.
3. El periodo mostraba el «hasta» en gris chico, como si fuera una acotación.
4. El perfil del funcionario en Mamoré no tenía forma de ver una licencia en
   detalle.

### Archivos modificados

| Archivo | Cambio |
|---|---|
| `app/Models/Licencia.php` | `solicitudesPendientes()`. |
| `app/Providers/AppServiceProvider.php` | Composer que le pasa el conteo al layout. |
| `resources/views/layouts/app.blade.php` | Aviso en la barra, con su estilo y el latido. |
| `resources/views/funcionarios/licencias-list.blade.php` | Sin columna Turno; periodo con las dos fechas. |
| `resources/views/licencias/list.blade.php` | Periodo con las dos fechas. |
| `app/Http/Controllers/PersonaController.php` | Sin `with('turno')`, que ya no se usa. |
| `app/Http/Controllers/Api/AsistenciaFuncionarioController.php` | `licencia()`: ficha con el desglose día por día. |
| `routes/api.php` | `GET funcionarios/{ci}/licencias/{licencia}`. |

### El aviso

Aparece a la derecha de la cuenta, con el número latiendo:

```
                              📋 Licencias pendientes (2)   [A] Admin ⌄
```

**Cuenta solicitudes, no filas**: un pedido de cinco días es uno. El enlace lleva
al listado ya filtrado por «Pendiente».

Solo se muestra **si hay alguna** —un cero permanente deja de mirarse a los dos
días— y **solo a quien puede resolverlas**: sin `ViewAny:Licencia` ni siquiera se
consulta. Va por `View::composer` para que la vista no toque la base.

Es barato: el `WHERE estado = 'Pendiente'` va por el índice `(estado, fecha)` y
toca unos pocos registros; el millón de aprobadas ni se roza.

El latido anima **solo el número** y con `transform` + `box-shadow`: animar el
bloque entero movería el renglón de la barra. Respeta `prefers-reduced-motion`.

### La ficha para el perfil

`GET funcionarios/{ci}/licencias/{licencia}` devuelve el pedido y su desglose día
por día —fecha, día de la semana, turno y estado—, que es lo que Mamoré muestra en
un modal. Es lo mismo que ve Recursos Humanos al abrir una solicitud, recortado a
lo que le sirve al funcionario: no van los avisos internos ni quién la revisó.

Se abre por **cualquiera** de sus días y siempre muestra el pedido entero. Mismo
candado que el respaldo y la baja: la cédula la pone el consumidor desde su
sesión, y acá se comprueba que la licencia le pertenezca —`404` si no, para no
confirmar que existe una licencia ajena con ese número—.

### Verificación

```
php artisan test  → 534/540 (los 6 de siempre)
pint → passed
```

Pruebas nuevas: que el aviso cuente solicitudes y no días, que desaparezca al no
haber ninguna, que no se muestre sin permiso; y que la ficha traiga los días, se
abra por cualquiera de ellos y devuelva 404 con la de otro funcionario.

---

## Trabajo 9 — Cada reloj se sincroniza solo, a las horas que se le configuren

**Commit:** `15cba62` — `feat(equipos): cada biométrico puede sincronizarse solo en los días y a las horas que se le configuren desde su ficha…`

### Problema

Las marcaciones entraban al sistema **solo si alguien las traía a mano**: apretando
«Sincronizar» en la ficha de cada equipo, o importando el CSV exportado del reloj.
Nadie lo hace tres veces al día, todos los días.

La consecuencia es la de siempre en este sistema: mientras la marcación no está en
`asistencias`, el cálculo de asistencia no la ve. Un funcionario que marcó entrada y
salida figura ausente hasta que alguien se acuerda de sincronizar.

Tampoco servía poner una hora fija en el código: los relojes están en lugares con
horarios distintos y quién opera el sistema tiene que poder cambiarla sin pedir un
despliegue.

### Archivos creados

| Archivo | Cambio |
|---|---|
| `database/migrations/2026_08_17_155537_agregar_sincronizacion_automatica_a_equipos.php` | `sync_automatica`, `sync_horarios` (JSON) y `sync_ultimo_automatico` en `equipos`. |
| `app/Services/SincronizadorEquipos.php` | Lectura del reloj + volcado a `asistencias` + bitácora, extraído de `EquipoController`. |
| `app/Console/Commands/SincronizarEquipos.php` | `sismark:sincronizar-equipos`, con `--equipo`, `--forzar`, `--desde`, `--hasta`. |
| `tests/Feature/SincronizarEquiposTest.php` | 11 pruebas del comando y del planificador. |

### Archivos modificados

| Archivo | Cambio |
|---|---|
| `app/Models/Equipo.php` | `horariosSync()`, `tocaSincronizar()`, `yaSincronizoEn()` y los casts nuevos. |
| `app/Http/Controllers/EquipoController.php` | Exportar y sincronizar delegan en el servicio; se le sacaron los dos métodos privados. |
| `app/Http/Requests/StoreEquipoRequest.php` · `UpdateEquipoRequest.php` | Validan el interruptor y las horas (`H:i`, al menos una si está encendido). |
| `resources/views/equipos/_form.blade.php` | Checkbox + repetidor de horas con Alpine. |
| `resources/views/equipos/show.blade.php` | Horas configuradas y última corrida automática. |
| `routes/console.php` | La tarea programada, cada minuto. |
| `Dockerfile` · `docker/entrypoint.sh` · `.env.docker.example` | El planificador dentro del contenedor. |

### Solución

**La configuración es por equipo, no global.** Cada reloj guarda su interruptor y su
lista de horas (`08:30`, `13:00`, `19:00`…), que se cargan desde el formulario del
equipo. Un reloj de portería y uno de un depósito no tienen por qué sincronizarse a
la misma hora, y ahora no hace falta que lo hagan.

**La tarea corre cada minuto y el comando decide quién trabaja.** El planificador no
puede saber los horarios de antemano —cambian desde la pantalla, sin tocar código—,
así que `routes/console.php` agenda el comando `* * * * *` y este compara el `HH:MM`
del momento contra la lista de cada equipo activo con la automática encendida. Son
pocos equipos y pocas horas: comparar en PHP sale más barato que inventar un filtro
en SQL sobre un JSON.

**El rango que se baja arranca en la última corrida automática**, no en el historial
completo del reloj. Si ayer el equipo estuvo caído, la corrida de hoy trae lo de
ayer y lo de hoy. Sin esto, cada corrida releería años de marcaciones para
descartarlas por duplicadas.

**`sync_ultimo_automatico` se escribe también cuando el reloj no contesta.** Es
«cuándo corrió la tarea», no «cuándo anduvo el equipo».

> **Reemplazado en el Trabajo 11.** Esa columna además decidía el «desde», y con
> las dos cosas en una sola marca un equipo caído perdía días para siempre. Hoy el
> rango sale de `sync_ultimo_exito`, que solo avanza cuando el reloj contestó.

**Nada de esto duplica marcaciones.** Todo pasa por `RegistroAsistencia`, el mismo
servicio del import de CSV y del botón manual: cruza el ID del reloj contra
`personas.ci`, descarta las fechas basura del RTC y no inserta lo que ya está
(mismo `ci` + `fecha` + `hora`). Repetir una hora en la lista no rompe nada.

**La bitácora registra cada corrida**, con la acción «Envió a la BD» y firmada como
**«Sistema»**: sin sesión no hay usuario, y `EquipoAuditoria::nombreUsuario()` ya
contemplaba ese caso.

### El servicio que hubo que extraer

`EquipoController` tenía la lectura del reloj y el filtro por rango como métodos
privados. Con un segundo disparador —la tarea— esa lógica pasó a
`App\Services\SincronizadorEquipos`, que ahora usan los dos: el botón de la ficha y
el comando. El controlador quedó con el mapeo de la respuesta y nada más; el
comportamiento del botón no cambió, y sus pruebas lo confirman sin tocarlas.

### Coolify: sin esto, los horarios no sirven de nada

El planificador de Laravel necesita que **algo** dispare `schedule:run` cada minuto.
En Coolify no hay crontab del host: el recurso es la imagen. Por eso el proceso vive
dentro del contenedor —`php artisan schedule:work`, levantado por el entrypoint como
usuario `unit`, con su log en `storage/logs/scheduler.log`—, igual que ya se hacía
con el microservicio de biométricos.

Viene encendido (`SISMARK_SCHEDULER=true` en el `Dockerfile`). **Se apaga solo si se
levanta más de una réplica de la imagen**: si no, cada réplica sincronizaría los
mismos relojes a la misma hora.

Lo que esto **no** resuelve: la ruta de red hasta los equipos. El contenedor sigue
necesitando alcanzar el puerto 4370 de cada reloj; si la aplicación corre en un VPS
fuera de la red del organismo, la tarea corre y falla igual que el botón.

### Verificación

```
php artisan schedule:list  → * * * * * php artisan sismark:sincronizar-equipos
php artisan test tests/Feature/SincronizarEquiposTest.php   → 11/11
php artisan test tests/Feature/EquipoControllerTest.php     → 53/53
php artisan test  → 554/560 (los 6 de siempre)
pint → passed
```

Las pruebas cubren lo que decide la tarea: que sincronice en la hora configurada,
que no haga nada fuera de ella, que saltee el equipo inactivo o con la automática
apagada, que no repita la corrida dentro del mismo minuto, que `--forzar` y
`--equipo` pasen por encima del horario, que el rango arranque en la última corrida,
que un equipo caído no tumbe al resto pero devuelva fallo, que la bitácora quede
firmada como «Sistema», y que el planificador tenga la tarea cada minuto.

### Pendiente

Cargar los horarios reales de cada reloj. La automática no se puede encender sin al
menos una hora, así que hasta que Recursos Humanos defina cuáles son, todo sigue
funcionando como hasta ahora: a botón.

---

## Trabajo 10 — La sincronización también se configura por días de la semana

**Commit:** `15cba62`

### Problema

El Trabajo 9 dejó la configuración a medias: solo horas. Un reloj puesto a las
19:00 le hablaba al equipo **sábado y domingo también**, cuando no hay nadie
marcando y no hay nada que traer.

No rompía nada —la corrida vuelve vacía—, pero es trabajo al pedo contra un aparato
que responde lento, y la ficha describía mal la jornada real: decía «19:00» donde
lo que corresponde es «lunes a viernes, 19:00».

### Archivos creados

| Archivo | Cambio |
|---|---|
| `database/migrations/2026_08_18_102437_agregar_dias_a_sincronizacion_de_equipos.php` | Columna `sync_dias` (JSON) en `equipos`. |

### Archivos modificados

| Archivo | Cambio |
|---|---|
| `app/Models/Equipo.php` | `diasSync()`, `nombresDiasSync()`, `tocaEnElDia()`; `tocaSincronizar()` exige día **y** hora. |
| `app/Http/Requests/StoreEquipoRequest.php` · `UpdateEquipoRequest.php` | Validan `sync_dias` contra `Turno::DIAS`. |
| `resources/views/equipos/_form.blade.php` | Las siete casillas de días. |
| `resources/views/equipos/show.blade.php` | La ficha dice los días junto con las horas. |
| `tests/Feature/SincronizarEquiposTest.php` · `EquipoControllerTest.php` | 12 pruebas nuevas. |

### Solución

**Se reusa la numeración que ya existía.** Los días van con los números de
`Turno::DIAS` (1 = Domingo … 7 = Sábado), que el sistema heredó del `DATEPART(dw)`
del SIA. Inventar una segunda convención de día de la semana en el mismo sistema
—una para turnos y otra para relojes— es la clase de detalle que después nadie
recuerda y termina en un turno corrido un día.

**Lista vacía significa «todos los días».** Los equipos configurados con el Trabajo
9 no tenían la columna, así que sin esa regla habrían dejado de sincronizarse al
aplicar la migración. El formulario los muestra con los siete tildados para que se
vea lo que va a pasar y no un campo en blanco que parece «ninguno».

**Los días guardados fuera de rango se descartan en vez de romper la corrida.** Un
`0` o un `9` en el JSON —de una carga vieja o de una edición a mano— se filtra en
`diasSync()`. La tarea corre igual con lo que quedó válido: un dato sucio no puede
dejar un reloj sin sincronizar sin que nadie se entere.

**Antes:**
```php
public function tocaSincronizar(Carbon $momento): bool
{
    if (! $this->activo || ! $this->sync_automatica) {
        return false;
    }

    return in_array($momento->format('H:i'), $this->horariosSync(), true);
}
```

**Después:**
```php
public function tocaSincronizar(Carbon $momento): bool
{
    if (! $this->activo || ! $this->sync_automatica) {
        return false;
    }

    if (! $this->tocaEnElDia($momento)) {
        return false;
    }

    return in_array($momento->format('H:i'), $this->horariosSync(), true);
}
```

### Verificación

Probado además contra el reloj real de la red (`192.168.14.187`), con el
planificador corriendo: a las `10:47:00` en punto la corrida trajo una marcación
nueva y quedó en la bitácora.

---

## Trabajo 11 — Un equipo caído recupera todo lo que se perdió

**Commit:** `15cba62`

### Problema

Salió de una revisión completa del sistema, no de un reporte: **el Trabajo 9 perdía
marcaciones en silencio**, y el caso que lo dispara es de los que pasan seguido —se
corta la luz, se desenchufa un switch, se cae la red del piso—.

`sync_ultimo_automatico` hacía dos trabajos incompatibles a la vez: registrar cuándo
corrió la tarea, y decidir desde cuándo pedirle datos al reloj. Como se escribía
aunque el equipo estuviera caído, la ventana avanzaba sola:

```
día 1  falla   → marca = día 1
día 2  falla   → pide desde el día 1, marca = día 2
día 3  anda    → pide desde el día 2   ← el día 1 no lo baja nadie, nunca
```

Un reloj caído cinco días volvía con los dos últimos. Los otros tres quedaban en su
buffer sin que nadie los pidiera, y el sistema los imputaba como faltas.

Los seis tests en rojo son otra cosa, y venían de antes: `DiaExcepcional` y
`DiaTurno` pasaron sus listados a carga por AJAX, pero las pruebas seguían buscando
las filas en la pantalla `index`, que hoy solo devuelve el marco.

### Archivos creados

| Archivo | Cambio |
|---|---|
| `database/migrations/2026_08_18_110132_agregar_ultimo_exito_a_sincronizacion_de_equipos.php` | Columna `sync_ultimo_exito`, con el valor viejo copiado. |

### Archivos modificados

| Archivo | Cambio |
|---|---|
| `app/Console/Commands/SincronizarEquipos.php` | `desdeDe()`, tope de 30 días, y la marca de éxito escrita solo en el éxito. |
| `app/Models/Equipo.php` | `sync_ultimo_exito` en `fillable` y casts. |
| `resources/views/equipos/show.blade.php` | «Último dato traído», con aviso si el equipo dejó de responder. |
| `tests/Feature/SincronizarEquiposTest.php` | 4 pruebas de recuperación. |
| `tests/Feature/DiaExcepcionalControllerTest.php` · `DiaTurnoControllerTest.php` | Los 6 tests rojos, apuntados al endpoint que sirve las filas. |
| `.gitignore` | `__pycache__/` y `*.py[cod]`; se sacó del versionado un `.pyc` del microservicio. |

### Solución

**Dos columnas, un trabajo cada una.**

| Columna | Qué guarda | Cuándo se escribe |
|---|---|---|
| `sync_ultimo_automatico` | Cuándo **corrió** la tarea | Siempre, ande o no el reloj |
| `sync_ultimo_exito` | Hasta cuándo se **trajo** información | Solo en el éxito. De acá sale el «desde» |

Mientras el equipo esté caído la ventana no se mueve, así que al volver se recupera
el hueco entero de una sola vez.

**Con tope de 30 días.** Sin límite, un equipo apagado seis meses volvería pidiendo
medio año de historial: el protocolo ZK tarda minutos en responder eso, la corrida
se encima con la del minuto siguiente y el reloj queda inservible mientras dura. Lo
que quede afuera se baja a mano con `--desde`, que no tiene tope. Cuando se recorta,
el comando lo avisa por consola en vez de hacerlo callado.

**La migración copia el valor viejo** a la columna nueva: la primera corrida después
de desplegar no sale a pedir el historial completo de cada reloj.

**La ficha muestra las dos fechas.** Es lo que delata al equipo que se «sincroniza»
todos los días sin traer nada —cable suelto, reloj colgado—, que con una sola fecha
parecía sano:

```
Última corrida automática   18/08/2026 13:00
Último dato traído          14/08/2026 19:00  [El equipo no responde desde entonces]
```

### Un detalle de las pruebas

El test que reproduce el bug —cuatro días de fallo y al quinto el reloj vuelve—
falló al primer intento: **`Http::fake()` acumula definiciones en vez de
reemplazarlas**, así que el `503` del principio seguía ganando cuando se declaraba
el fake del éxito. Se resolvió con un único stub y una bandera capturada por
referencia.

### Verificación

```
php artisan test  → 578/578 ✅   (era 554/560)
pint → passed
```

Las pruebas nuevas cubren: que el fallo no mueva la marca de datos, que una caída de
cuatro días recupere las tres marcaciones del hueco, que un equipo nuevo arranque
por hoy en vez de bajar el historial de sorpresa, y que una caída larguísima se
corte en 30 días.

Es la primera vez que la suite queda entera en verde.

---

## Nota — Verificación de S3

Se comprobó de punta a punta que los respaldos llegan al bucket, a pedido, antes
de empezar a probar en serio:

```
Config      bucket=sismark · root=dev · endpoint=gadbeni.sfo3.digitaloceanspaces.com
            path_style=true · throw=true

Directo     put() OK · size OK · get() devuelve lo escrito · firma OK · delete OK

Flujo real  POST licencia con PDF        → 201
            GET enlace firmado           → https://…/sismark/dev/licencias/2026/7633685/…pdf
            descarga desde DigitalOcean  → HTTP 200 · 595 bytes · IDENTICO al original
            misma URL sin firma          → HTTP 403
```

También por la cadena de Mamoré (`SismarkClient::solicitarLicencia` con archivo):
el PDF quedó en `licencias/2026/7633685/…`, con `origen = mamore` y contenido
idéntico. **Mamoré no escribe en S3**: reenvía el archivo y lo guarda este sistema
con sus propias variables.

Los tres puntos de subida —licencia desde la API, licencia desde la pantalla y día
excepcional— pasan por `RespaldoDocumento`, que solo conoce el disco `s3`. No hay
ruta alternativa a disco local, y el disco va con `throw => true`: si la subida
falla, salta excepción en vez de dejar una fila diciendo que tiene respaldo cuando
el archivo nunca llegó.

**Pendiente de configuración:** el `.env` tiene el bloque `AWS_*` duplicado
—líneas 70-72 con `AWS_BUCKET` vacío y 93-97 con los valores reales—. Hoy gana el
último y funciona, pero conviene borrar el vacío antes de que muerda.

---

## Informe para presentación (copiar/pegar en Word)

**Informe de trabajo — 18/08/2026**
**Responsable:** nachoDev

**Actividad:** Se completó el circuito de solicitudes de licencia: el funcionario
ahora puede adjuntar el respaldo de su pedido, y Recursos Humanos cuenta con la
pantalla para revisarlo y aprobarlo o rechazarlo.

**Problema detectado:**

Los funcionarios ya podían solicitar una licencia desde su perfil, pero esas
solicitudes quedaban esperando sin que nadie pudiera resolverlas: no existía la
pantalla para aprobarlas ni se veía en el listado cuáles estaban pendientes.

Eso tenía una consecuencia concreta: como el sistema solo descuenta las licencias
aprobadas, cada pedido quedaba sin efecto y la ausencia se le seguía contando como
falta al funcionario, que mientras tanto veía su solicitud registrada y creía que
estaba en trámite.

Además, el funcionario no tenía cómo adjuntar el certificado médico o la nota que
justifica su pedido, de modo que quien debía aprobarlo no tenía con qué decidir.

**Solución aplicada:**

Al solicitar la licencia, el funcionario puede ahora adjuntar un respaldo
—certificado, memorándum o nota, en imagen o PDF—. Es opcional, porque el
documento suele presentarse al regresar; quien aprueba decide si le alcanza con lo
que ve.

En el sistema de biométrico, las licencias tienen una ficha donde se ve el pedido
completo antes de decidir: los días que abarca, el motivo, el respaldo adjunto y
quién lo pidió. Desde ahí se aprueba o se rechaza. Al rechazar se exige escribir el
motivo, que es el que después lee el funcionario en su perfil.

Cada resolución queda registrada con el nombre de quien la tomó y la fecha, y el
listado de licencias suma una columna de estado y un filtro para encontrar
rápidamente las que están pendientes de revisión.

Se corrigió además cómo se ven las licencias de varios días. Un permiso del 14 al
15 de agosto se mostraba como dos licencias separadas, porque el sistema guarda un
registro por cada día, y eso daba a entender que había que aprobarlo dos veces.
Ahora el listado muestra **una sola licencia** con su periodo («14/08 al 15/08») y
la cantidad de días, y se aprueba de una vez. El detalle día por día sigue estando
dentro de la ficha, y el registro interno no cambió: sigue siendo por día, que es
lo que necesita el cálculo de asistencia.

**Detalle de archivos modificados:**
1. Servicio de solicitudes de licencia: acepta el archivo de respaldo y lo
   almacena de forma segura.
2. Pantalla de licencias: ficha nueva con el detalle del pedido y los botones para
   aprobar o rechazar.
3. Listado de licencias: columna de estado y filtro por estado.
4. Tabla de licencias: se registra quién resolvió cada solicitud y cuándo.
5. Permisos del sistema: se agregaron «Ver ficha» y «Aprobar» para licencias, de
   modo que se pueda dar el permiso de revisar sin dar el de decidir.
6. Listado de licencias: los días de un mismo permiso se muestran como una sola
   licencia, con su periodo y su cantidad de días.
7. Servicio de consulta para el perfil del funcionario: entrega las licencias
   agrupadas por permiso, permite abrir el respaldo presentado, ver el detalle
   día por día y dar de baja un pedido que todavía nadie revisó.
8. Registro de licencias: un permiso rechazado ya no deja el día bloqueado, y los
   pedidos anteriores se conservan como historial.
9. Barra superior: aviso con la cantidad de solicitudes esperando revisión.

Desde su perfil, el funcionario ve además cada permiso como uno solo aunque abarque
varios días, puede abrir el certificado que presentó, y puede dar de baja un pedido
que todavía nadie revisó —por ejemplo si se equivocó de fecha—, dejando anotado el
motivo. Esa baja no borra nada: el registro queda guardado con la fecha y el motivo,
y solo alcanza a lo que aún está pendiente de revisión.

**Resultado:** El circuito queda cerrado de punta a punta: el funcionario pide su
licencia con el respaldo correspondiente, la corrige o la retira si se equivocó,
Recursos Humanos la revisa y la resuelve, y recién ahí la ausencia queda
justificada. Toda decisión queda registrada con su responsable.

---

**Informe de trabajo — 18/08/2026 (segundo)**
**Responsable:** nachoDev

**Actividad:** Los relojes biométricos ahora se descargan solos, a las horas que se
le configuren a cada equipo desde el sistema.

**Problema detectado:**

Las marcaciones de los relojes entraban al sistema únicamente cuando una persona
las traía a mano, apretando «Sincronizar» en la ficha del equipo o importando el
archivo exportado del reloj.

Eso tenía una consecuencia concreta sobre el control de asistencia: mientras la
marcación no está cargada, el sistema no la ve, y el funcionario que marcó su
entrada y su salida figura como ausente hasta que alguien se acuerde de
sincronizar. Depender de que una persona lo haga varias veces al día, todos los
días, no es sostenible.

Poner una hora fija en el programa tampoco servía: los equipos están en lugares con
horarios de trabajo distintos, y quien administra el sistema tiene que poder
cambiar esa hora sin depender del desarrollador.

**Solución aplicada:**

En la ficha de cada equipo biométrico se agregó la opción **«Sincronizar
automáticamente a horas fijas»**, con la lista de horas que corresponda a ese
equipo —por ejemplo 08:30, 13:00 y 19:00, una vez cerrado cada bloque de
marcación—. A esas horas el sistema se conecta solo al reloj y descarga las
marcaciones nuevas, sin intervención de nadie.

Cada equipo tiene su propia configuración: un reloj de portería y uno de un
depósito pueden trabajar con horarios distintos. La opción viene apagada, de modo
que un equipo recién registrado no empieza a descargarse solo hasta que se lo
decide.

Cada descarga automática queda registrada en la bitácora de equipos, con la fecha,
la cantidad de marcaciones traídas y el resultado, identificada como hecha por el
«Sistema». Las marcaciones no se duplican: si una ya estaba cargada, se descarta,
igual que en la carga manual. Si un reloj está apagado o fuera de red, la descarga
siguiente recupera lo que quedó pendiente, y el resto de los equipos no se ve
afectado. El botón para sincronizar a mano sigue estando y funciona igual.

**Detalle de archivos modificados:**
1. Ficha del equipo biométrico: opción de sincronización automática y sus horarios.
2. Tabla de equipos: se guardan el interruptor, las horas y la fecha de la última
   descarga automática.
3. Tarea automática del sistema: revisa cada minuto qué equipos tienen configurada
   esa hora y descarga sus marcaciones.
4. Servicio de sincronización: la descarga desde el reloj y su registro pasan a
   estar en un solo lugar, usado tanto por el botón manual como por la tarea.
5. Configuración del servidor: el sistema levanta por su cuenta el proceso que
   dispara las tareas programadas, sin depender de configuración externa.

**Resultado:** El control de asistencia deja de depender de que alguien recuerde
descargar los relojes. Las marcaciones llegan solas a las horas definidas, y los
informes reflejan la asistencia real del día sin trabajo manual de por medio.

**Pendiente:** definir con Recursos Humanos las horas de descarga de cada equipo y
cargarlas. Hasta entonces el sistema sigue funcionando como hasta ahora, con la
descarga manual.

---

*Documento de sesión — nachoDev — Gobierno Autónomo Departamental del Beni.*
